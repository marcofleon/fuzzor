[package]
name = "types"
version = "0.1.0"
edition = "2021"

[profile.release]
strip = "symbols"

[dependencies]
serde = { version = "1.0.197", features = ["derive"] }
serde_with = { version = "3.8.1", features = ["base64"] }

[lib]
name = "types"
path = "src/lib.rs"
